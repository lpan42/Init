netstat -rn | grep 'default' | sed q | awk '{print $2}'

# Identify the default gateway in the routing table

# default gateway 默认网关 网络出口
# netstat: show network status
# -r    Show the routing tables